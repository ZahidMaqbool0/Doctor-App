import 'package:flutter/material.dart';

class MyHealthScreen extends StatelessWidget {
  const MyHealthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My Health Screen'),);
  }
}
